[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python -m bot.main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

# Railway memory limits for deployment
[deploy.memory]
limit = "512Mi"
swapLimit = "256Mi"
